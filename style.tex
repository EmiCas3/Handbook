% =============================================================
%  style.tex  -  Handbook visual style
% =============================================================

% -----------------------------
% THEME SYSTEM (Makefile driven)
% -----------------------------
\providecommand{\THEME}{ide}

\ifdefined\inputtheme
  \renewcommand{\THEME}{\inputtheme}
\fi


% ------------------------------------------------------------------
% 1. TYPOGRAPHY
% ------------------------------------------------------------------
\usepackage[protrusion=true,expansion=false]{microtype}

\IfFileExists{inconsolata.sty}{%
  \usepackage[scaled=0.92]{inconsolata}%
}{%
  \usepackage{courier}%
}

\usepackage{graphicx}
\setkeys{Gin}{width=\linewidth,totalheight=\textheight,keepaspectratio}

% ------------------------------------------------------------------
% 2. SPACING
% ------------------------------------------------------------------
\setlength{\parindent}{0pt}
\setlength{\parskip}{2.5mm}

% ------------------------------------------------------------------
% 3. THEME SYSTEM
\usepackage{xcolor}
%    Override from Makefile with: make THEME=print / dark / blue / ide
%    Default theme: ide  (purple keywords, dark-red strings, green comments)
% ------------------------------------------------------------------

% \THEME is injected by the Makefile via \def\THEME{...}
% If not set, fall back to "ide"
\providecommand{\THEME}{ide}

% ---- ide (default) -----------------------------------------------
\definecolor{ide-kw}     {HTML}{7B2FBE}  % purple   - keywords
\definecolor{ide-str}    {HTML}{A31515}  % dark red  - strings
\definecolor{ide-cmt}    {HTML}{008000}  % green     - comments
\definecolor{ide-base}   {HTML}{000000}  % black     - body text

% ---- print (bold black, no color - safe for B&W laser) -----------
\definecolor{print-kw}   {gray}{0.0}
\definecolor{print-str}  {gray}{0.0}
\definecolor{print-cmt}  {gray}{0.45}
\definecolor{print-base} {gray}{0.0}

% ---- blue --------------------------------------------------------
\definecolor{blue-kw}    {HTML}{003087}
\definecolor{blue-str}   {HTML}{0066CC}
\definecolor{blue-cmt}   {HTML}{5588AA}
\definecolor{blue-base}  {HTML}{000000}

% ---- dark (dark background, light text) --------------------------
\definecolor{dark-kw}    {HTML}{569CD6}  % light blue
\definecolor{dark-str}   {HTML}{CE9178}  % orange
\definecolor{dark-cmt}   {HTML}{6A9955}  % green
\definecolor{dark-base}  {HTML}{D4D4D4}  % light gray
\definecolor{dark-bg}    {HTML}{1E1E1E}  % VS Code dark bg
\definecolor{dark-border}{HTML}{3C3C3C}

% Shared non-theme colors
\definecolor{codebg}     {HTML}{F8F8F8}
\definecolor{codeborder} {HTML}{CCCCCC}
\definecolor{rowgray}    {HTML}{F2F2F2}
\definecolor{headergray} {HTML}{D9D9D9}
\definecolor{linkcolor}  {HTML}{0550AE}

% Resolve active theme colors into single names used by listings
\def\themeIde{ide}
\def\themePrint{print}
\def\themeBlue{blue}
\def\themeDark{dark}

\ifx\THEME\themeIde
  \colorlet{kw}      {ide-kw}
  \colorlet{str}     {ide-str}
  \colorlet{cmt}     {ide-cmt}
  \colorlet{codebody}{ide-base}
  \colorlet{activebg}{codebg}
  \colorlet{activeborder}{codeborder}
\else\ifx\THEME\themePrint
  \colorlet{kw}      {print-kw}
  \colorlet{str}     {print-str}
  \colorlet{cmt}     {print-cmt}
  \colorlet{codebody}{print-base}
  \colorlet{activebg}{codebg}
  \colorlet{activeborder}{codeborder}
\else\ifx\THEME\themeBlue
  \colorlet{kw}      {blue-kw}
  \colorlet{str}     {blue-str}
  \colorlet{cmt}     {blue-cmt}
  \colorlet{codebody}{blue-base}
  \colorlet{activebg}{codebg}
  \colorlet{activeborder}{codeborder}
\else\ifx\THEME\themeDark
  \colorlet{kw}      {dark-kw}
  \colorlet{str}     {dark-str}
  \colorlet{cmt}     {dark-cmt}
  \colorlet{codebody}{dark-base}
  \colorlet{activebg}{dark-bg}
  \colorlet{activeborder}{dark-border}
\else
  % Unknown theme - fall back to ide
  \colorlet{kw}      {ide-kw}
  \colorlet{str}     {ide-str}
  \colorlet{cmt}     {ide-cmt}
  \colorlet{codebody}{ide-base}
  \colorlet{activebg}{codebg}
  \colorlet{activeborder}{codeborder}
\fi\fi\fi\fi



% ------------------------------------------------------------------
% 4. HYPERREF
% ------------------------------------------------------------------
\usepackage{hyperref}
\hypersetup{
  colorlinks = true,
  linkcolor  = linkcolor,
  urlcolor   = linkcolor,
  citecolor  = linkcolor,
  pdftitle   = {Competitive Programming Handbook},
  pdfauthor  = {Felipe Rafael Focil Mendoza},
}
\usepackage{bookmark}

% ------------------------------------------------------------------
% 5. CODE BLOCKS
%    Uses a custom CPP language to avoid listings' broken directive
%    parser which conflicts with C++ template angle-brackets <>.
% ------------------------------------------------------------------
\usepackage{listings}
\usepackage{fvextra}
\usepackage[most]{tcolorbox}

% Custom CPP language: no preprocessor directives, safe with templates
\lstdefinelanguage{CPP}{
  sensitive   = true,
  morecomment = [l]{//},
  morecomment = [s]{/*}{*/},
  morestring  = [b]",
  morestring  = [b]',
  keywords    = {auto,break,case,catch,class,const,constexpr,continue,
                 default,delete,do,else,enum,explicit,extern,false,for,
                 friend,goto,if,inline,int,long,mutable,namespace,new,
                 noexcept,nullptr,operator,private,protected,public,
                 register,return,short,signed,sizeof,static,struct,
                 switch,template,this,throw,true,try,typedef,typename,
                 union,unsigned,using,virtual,void,volatile,while,
                 bool,char,double,float,string,vector,map,set,pair,
                 queue,stack,deque,list,array,bitset,priority_queue,
                 unordered_map,unordered_set,multimap,multiset,
                 ll,vi,vvi,vl,pi,u64,ull,define,include},
}

\lstdefinestyle{cppstyle}{
  language         = CPP,
  basicstyle       = \ttfamily\small\color{codebody},
  keywordstyle     = \bfseries\color{kw},
  stringstyle      = \color{str},
  commentstyle     = \itshape\color{cmt},
  numbers          = left,
  numberstyle      = \tiny\ttfamily\color{cmt},
  numbersep        = 8pt,
  breaklines       = true,
  breakatwhitespace= true,
  tabsize          = 4,
  showstringspaces = false,
  frame            = none,
  aboveskip        = 0pt,
  belowskip        = 0pt,
  xleftmargin      = 0pt,
  % Spanish / Greek chars in code comments
  literate =
    {á}{{\'{a}}}1 {é}{{\'{e}}}1 {í}{{\'{i}}}1 {ó}{{\'{o}}}1 {ú}{{\'{u}}}1
    {Á}{{\'{A}}}1 {É}{{\'{E}}}1 {Í}{{\'{I}}}1 {Ó}{{\'{O}}}1 {Ú}{{\'{U}}}1
    {ñ}{{\~{n}}}1 {Ñ}{{\~{N}}}1
    {θ}{{\ensuremath{\theta}}}1,
}

% \codeInput{filename} - renders a .verbatim file in a styled box
\newcommand{\codeInput}[1]{%
  \tcbinputlisting{
    listing only,
    listing options = {style=cppstyle},
    colback  = activebg,
    colframe = activeborder,
    arc      = 3pt,
    boxrule  = 0.5pt,
    left=6pt, right=6pt, top=4pt, bottom=4pt,
    before skip=4pt, after skip=6pt,
    breakable,
    listing file = {#1}
  }%
}

% Inline code pill
\newcommand{\codeinline}[1]{%
  \tcbox[on line,
    colback=activebg, colframe=activeborder,
    arc=2pt, boxrule=0.4pt,
    left=1pt, right=1pt, top=0pt, bottom=0pt,
    boxsep=1pt]{\ttfamily\small\color{codebody} #1}}

% ------------------------------------------------------------------
% 6. TABLES
% ------------------------------------------------------------------
\usepackage{colortbl}
\usepackage{array}
\usepackage{etoolbox}

\newcommand{\mdtabularopen}[1]{%
  \begin{center}%
  \rowcolors{2}{rowgray}{white}%
  \setlength{\arrayrulewidth}{0.6pt}%
  \setlength{\tabcolsep}{6pt}%
  \renewcommand{\arraystretch}{1.25}%
  \begin{tabular}{#1}%
}
\newcommand{\mdtabularclose}{\end{tabular}\end{center}}

\newcolumntype{H}{>{\columncolor{headergray}\centering\arraybackslash}c}
\newcolumntype{C}{>{\centering\arraybackslash}c}

\newenvironment{mdtable2}[1][]{\mdtabularopen{|H|C|}}       {\mdtabularclose}
\newenvironment{mdtable3}[1][]{\mdtabularopen{|H|C|C|}}     {\mdtabularclose}
\newenvironment{mdtable4}[1][]{\mdtabularopen{|H|C|C|C|}}   {\mdtabularclose}
\newenvironment{mdtable5}[1][]{\mdtabularopen{|H|C|C|C|C|}} {\mdtabularclose}

% (Markdown rendering handled by pandoc + handbook.lua filter)